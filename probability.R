# Probability

# P(A) = |A|/|U|

# pdf: P {a < x < b} = \int_a^b g(x) dx

# Properties: 
# 0 <= P(x) <= 1
# \int_-\inf^\inf = 1, use a normalizing term to ensure

# PDF(X) -> X_O
# P(\theta | x) \neq P(x | \theta)
# L (theta | x) \propto P(x | \theta)
# LS is PDF, RS is likelihood function



